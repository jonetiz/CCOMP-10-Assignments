{
    "version": "2.0.0",
    "configurations": [
        {
          "type": "node",
          "request": "launch",
          "name": "launch program",
          "skipFiles": [ "<node_internals>/**"],
          "preLaunchTask": "compile and run pde",
          "program": "${workspaceFolder}/test.js"
        }
    ]
  }